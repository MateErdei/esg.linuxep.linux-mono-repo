load("//tools/config:soph_rules.bzl", "soph_cc_library")

soph_cc_library(
    name = "OSUtilities",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    visibility = [
        # OSUtilities is not a full implementation
        # BUILD files should depend on OSUtilitiesImpl
        "//modules/Common/OSUtilitiesImpl:__pkg__",
    ],
)
