load("//tools/config:soph_rules.bzl", "soph_cc_library")

soph_cc_library(
    name = "ZMQWrapperApi",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    visibility = [
        # ZMQWrapperApi is not a full implementation
        # BUILD files should depend on ZMQWrapperApiImpl
        "//modules/Common/ZMQWrapperApiImpl:__pkg__",
    ],
    deps = [
        "//modules/Common/ZeroMQWrapperImpl",
    ],
)
