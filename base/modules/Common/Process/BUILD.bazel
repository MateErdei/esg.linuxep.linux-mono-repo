load("//tools/config:soph_rules.bzl", "soph_cc_library")

soph_cc_library(
    name = "Process",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    visibility = [
        # Process is not a full implementation
        # BUILD files should depend on ProcessImpl
        "//modules/Common/ProcessImpl:__pkg__",
    ],
)
