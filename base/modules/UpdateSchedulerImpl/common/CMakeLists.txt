INCLUDE_DIRECTORIES(SYSTEM BEFORE PUBLIC ${PROTOBUF_INCLUDE_DIR})

PROTOBUF_GENERATE_CPP(STATE_MACHINE_DATA_PROTO_SRC STATE_MACHINE_DATA_PROTO_H StateMachineData.proto)

add_library(StateMachineDataProto OBJECT
        ${STATE_MACHINE_DATA_PROTO_SRC}
        ${STATE_MACHINE_DATA_PROTO_H}
        )