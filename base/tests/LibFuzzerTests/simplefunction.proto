syntax = "proto2";
package SimpleFunctionProto;

message TestCase {
    optional string payload = 1;
    enum FunctionTarget
    {
        enforceUTF8 = 1;
        jsonToMap = 2;
        regexUtility = 3;
    }
    required FunctionTarget functionTarget = 2;
}