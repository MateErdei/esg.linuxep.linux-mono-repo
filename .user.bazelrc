# Our code is currently C++17
build --features=-cxx20

# The SPL_BAZEL define is used to conditionally select code for Bazel builds (toolchain differences etc.)
build --copt=-DSPL_BAZEL

# Add global include search paths
# 'base' is needed for tests/ include paths to work currently
build --define SOPH_ASSET_SOURCE_ROOT=base/modules;base;eventjournaler/modules;liveterminal/modules;av/modules;av;edr/modules

common --experimental_cc_shared_library
