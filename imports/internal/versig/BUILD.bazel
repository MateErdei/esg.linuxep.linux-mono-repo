# Copyright 2023 Sophos Limited. All rights reserved.

genrule(
    name = "extract_versig_binary",
    srcs = ["versig.tar"],
    outs = ["versig"],
    cmd_bash = "tar -xf $< --strip-components=2 -C $(RULEDIR)",
    visibility = [
        "//base/products/Base/installer:__pkg__",
        "//thininstaller:__pkg__",
    ],
)
